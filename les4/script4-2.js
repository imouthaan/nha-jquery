let counter = 10;
const number = 10;

const result = document.getElementById('divResult');

while (i > 0) {
    result.innerHTML += i + ' maal ' + number + ' = ' + i * number + '<br />';
    i -= 1;
}
