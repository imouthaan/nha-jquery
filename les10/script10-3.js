const button = $('#sprite');
const dom = $(document);

dom.on('keydown', (e) => {
    switch (e.key) {
        case 'value':

            break;

        default:
            console.log(e.key);
            break;
    }
})
